# Session Handoff — 2026-04-08 Trail 0.3% 실전 적용 + 7개 개선

## Status

### 실전 거래 진행 중
- **전략**: Gap FADE + Trailing Stop 0.3% (백테스트 최적)
- **종목**: GOOGL, NVDA, TSLA (바이낸스 선물 Perp)
- **레버리지**: 10x, ISOLATED 마진
- **총 자산**: ~$89 (원금 $100 대비 -11%)
- **현재 포지션**: GOOGL SHORT 1.0주 (PnL ~+$1.5)
- **오늘 청산**: TSLA LONG -$7.21, NVDA LONG -$6.19 (trail 발동)
- **서버**: 159.65.6.136 (DigitalOcean $6/mo, Singapore)

### 이번 세션 완료 사항
1. **TP/SL 백테스트 전면 실시** — 8종목 × 13개 청산 전략 × 5개 종목 조합
2. **Trailing Stop이 모든 전략 중 압도적** 발견
   - Trail 0.3%, GOOGL+NVDA, 100%: $1K → $300K (9개월, +29,912%)
   - Trail 0.5%: $1K → $35.6K
   - EOD only: $1K → $3.8K
3. **Trail 0.3% 실전 적용** — 0.5초 간격 가격 체크, 서버에서 실시간 가동
4. **7개 개선 사항 일괄 적용**:
   - 대시보드 trail 실시간 표시 (peak, drop%, 프로그레스바)
   - 일일 리포트 자동화 (장마감 시 DB + 텔레그램)
   - DB 거래 기록 (trail 청산 시 자동 저장)
   - 종목 자동 선별 함수 (90일 FADE 승률 기반)
   - 시장 환경 필터 (SPY MA20 + 변동성)
   - trail_state 파일 공유 (trailer ↔ dashboard 프로세스 간)
   - 백테스트 기본값 변경 (GOOGL+NVDA, 10x)
5. **자금 투입 비율 백테스트** — 100% 투입이 최적 (trail이 손실 제한)
6. **기존 거래 DB 입력** — TSLA/NVDA/GOOGL 테스트 거래 + 수수료
7. **SSH 키 설정** — ~/.ssh/gapfade_server로 서버 직접 접속 가능
8. **자동 배포** — git push → 1분 내 서버 자동 pull + restart

## Key Decisions

1. **Trail 0.3% 채택** — 0.5%보다 승률 70% vs 57%, 수익 10배 차이
2. **100% 자본 투입 유지** — trail이 손실 0.3%로 제한하므로 풀베팅이 합리적
3. **GOOGL+NVDA 유지** — TSLA는 FADE 안 먹힘 (모멘텀 종목), 하지만 config에는 포함 상태
4. **Score 가중 안 함** — 100% 고정이 수익 극대화 (score 가중 시 수익 1/3)
5. **시장 필터** — SPY MA20 하회 + 고변동성 시 거래 중단 (scanner.py에 구현)

## Open Issues

1. **TSLA 문제**: FADE 전략에 안 맞음 (모멘텀 종목). config에서 빼야 할 수 있음
2. **trail_state 대시보드 표시**: trail API는 동작하나 대시보드 UI에서 보이는지 실제 확인 필요
3. **재진입 로직 미구현**: trailer.py에 코드 없음. trail 청산 후 같은 날 재진입 가능하면 거래 빈도 증가
4. **백테스트 대시보드(port 8000)에 trail 옵션 미반영**: API 기본값만 변경, UI 슬라이더 미추가
5. **펀딩피 표시**: 바이낸스 income API에서 FUNDING_FEE 가져올 수 있으나 대시보드에 미반영
6. **서버 systemd timer**: deploy.sh의 gapfade-scan.timer가 main.py run 실행 → trailer 포함. 하지만 서버 재부팅 시 timer가 제대로 작동하는지 미검증
7. **과적합 우려**: Trail 0.3%가 9개월 백테스트에서 극단적 수익. 실전에서 동일할지 검증 필요
8. **봇 잔고 경고**: 현재 총자산 ~$89. 계속 손실 시 포지션 크기가 줄어들어 수수료 비율 증가

## Next Steps

1. **오늘 밤 22:35 KST 자동 거래 확인** — 첫 trail 0.3% 실전 사이클
2. **1주일 실전 결과 모니터링** — 승률, P&L, trail 발동 빈도 추적
3. **TSLA 제거 여부 결정** — 1주일 데이터 보고 판단
4. **재진입 로직 구현** — trail 청산 후 가격이 다시 유리해지면 재진입
5. **백테스트 대시보드에 trail 슬라이더 추가**
6. **대시보드에 펀딩피 표시 추가**
7. **종목 자동 선별 활성화** — auto_select_tickers() 주간 실행

## Files Modified (이번 세션)

### 신규
- `bot/trailer.py` — Trailing Stop 모니터 v2 (0.3%, 0.5초, 파일 상태 공유, DB 저장)
- `bot/trail_state.json` — trailer ↔ dashboard 실시간 상태 (gitignore)

### 수정
- `bot/live_dashboard.py` — 한글화, 전략 상세, trail 실시간 UI, 현재가 수정, 총자산=잔고+미실현
- `bot/bn_executor.py` — ISOLATED 마진 강제, 체결가 0 수정, set_isolated()
- `bot/scanner.py` — check_market_ok(), auto_select_tickers()
- `bot/notifier.py` — trade_closed 가격 0 방어, 전체 한글화
- `bot/main.py` — trail 명령 추가, run=scan+execute+trail
- `bot/backtest_api.py` — 기본값 GOOGL+NVDA, 10x
- `bot/config.py` — TICKERS=GOOGL,NVDA,TSLA, LEVERAGE=10
- `deploy.sh` — gapfade-scan을 simple 타입으로 (trailer 포함)

## Context

- **서버 SSH**: `ssh -i ~/.ssh/gapfade_server root@159.65.6.136`
- **자동 배포**: git push → 서버 /root/auto-deploy.sh (cron 1분마다)
- **대시보드**: http://159.65.6.136:8001
- **백테스트 대시보드**: 로컬 `python3 bot/backtest_api.py` → localhost:8000
- **텔레그램**: @Gapfadebot, chat_id=2107749008
- **바이낸스**: TradFi Perps 약관 동의 완료, ISOLATED 마진, 선물 잔고 ~$89
- **Hyperliquid**: 0xbBb7D077...Db9853, USDC $96.45 (현재 미사용)
- **리포**: https://github.com/wkdgks150/krx-perp-arb-study (public)
- **이전 핸드오프**: .claude/handoffs/2026-04-08-2000-trading-bot-live.md
- **DigitalOcean 콘솔 주의**: heredoc(<<) 안 먹힘, echo로 한 줄씩
- **사용자 선호**: 최대한 공격적, 100% 투입, 총자산(잔고+미실현)으로 항상 얘기
